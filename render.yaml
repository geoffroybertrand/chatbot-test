services:
  - type: web
    name: mon-superviseur-ia
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app/interface/main.py --server.port=$PORT --server.address=0.0.0.0
    envVars:
      - key: CLAUDE_API_KEY
        sync: false